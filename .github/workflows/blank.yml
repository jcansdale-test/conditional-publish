on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        if: ${{ env.FOO == 'FOO' }}
        run: echo Publish using secret ${{ env.FOO }}
        env:
          FOO: ${{ secrets.FOO }}
